<ui:BaseView x:Class="VGA.Editor.Views.EditView"
             xmlns:viewModels="clr-namespace:VGA.Editor.ViewModels"
             x:TypeArguments="viewModels:EditViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:ui="clr-namespace:Common.UI;assembly=Common.UI"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:VGA.Editor.Views"
             Background="#6a6969">
    <Grid>
        <local:EmployeeEditView DataContext="{Binding EmployeeEditViewModel}"/>
        <local:OrderEditView DataContext="{Binding OrderEditViewModel}"/>
    </Grid>
</ui:BaseView>
